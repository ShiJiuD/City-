# ciTY展馆 — 接口文档（首页展览模块）

### 一、响应格式规范

所有接口统一返回 `Result<T>` 结构：

```json
{
  "code": 1,       // 1=成功，0=失败
  "msg": "操作成功", // 提示文案
  "data": {}        // 业务数据
}
```

| code 值 | 含义 |
|---------|------|
| `1` | 成功 |
| `0` | 失败 |

### 二、接口路径规范

- **全部接口强制带 `/api/` 前缀**
- C端：`/api/app/...`
- B端：`/api/admin/...`
- 首页浏览为 C 端行为，统一用 `/api/app/...`

### 三、JSON 字段命名

- 字段名统一使用 **camelCase**（小驼峰）
- 数据库字段为 `snake_case`，MyBatis-Plus 自动映射（`map-underscore-to-camel-case: true`）

---

## 一、获取首页聚合数据 🔓

> 一次性返回顶部 Banner、中部"当前热门展览"横条、底部"热门美术馆"列表。前端调用一次即可填满整个首页首屏。

- **Path**：`/api/app/home`
- **Method**：`GET`
- **描述**：首页首屏聚合接口，返回 Banner 轮播、热门展览、热门美术馆三块数据

**参数**：

| 参数名 | 类型 | 必须 | 默认值 | 说明 |
|--------|------|------|--------|------|
| city | String | 否 | - | 城市筛选（如：南京）。不传则返回全部城市数据 |

**成功返回**：

```json
{
  "code": 1,
  "msg": "查询成功",
  "data": {
    "banners": [
      {
        "id": 1,
        "imageUrl": "https://oss.example.com/banner_waterprint.jpg",
        "title": "江苏水印木刻实践与研究特展"
      }
    ],
    "hotExhibitions": [
      {
        "id": 101,
        "posterImage": "https://oss.example.com/exhibit_yihai.jpg",
        "title": "艺海同辉",
        "subtitle": "SHARED ARTISTIC BRILLIANCE",
        "galleryName": "江苏省美术馆",
        "type": 1
      }
    ],
    "galleries": [
      {
        "id": 1,
        "name": "江苏省美术馆",
        "coverImage": "https://oss.example.com/gallery_js.jpg",
        "address": "南京·秦淮区四条巷12号",
        "exhibitionCount": 1,
        "type": 0
      }
    ]
  }
}
```

**失败返回**：

```json
{
  "code": 0,
  "msg": "系统异常",
  "data": null
}
```

---

## 二、"艺览智荐"推荐展览列表

> ⚠️ **此板块由 AI 团队负责**
>
> 推荐逻辑依赖用户行为数据（浏览量、收藏记录等）
>
> 此接口 **待确定方案后再补充**。

---

## 三、获取美术馆列表 🔓

> 用户点击首页底部"查看更多"或进入独立"美术馆"页面时调用。

- **Path**：`/api/app/galleries`
- **Method**：`GET`
- **描述**：查询美术馆列表，支持城市筛选和名称搜索

**参数**：

| 参数名 | 类型 | 必须 | 默认值 | 说明 |
|--------|------|------|--------|------|
| city | String | 否 | - | 城市筛选 |
| keyword | String | 否 | - | 美术馆名称模糊搜索 |

**成功返回**：

```json
{
  "code": 1,
  "msg": "查询成功",
  "data": [
    {
      "id": 3,
      "name": "四方美术馆",
      "coverImage": "https://oss.example.com/gallery_sifang.jpg",
      "address": "南京·浦口区珍七路9号(佛手湖)",
      "exhibitionCount": 0,
      "type": 0
    }
  ]
}
```

**失败返回**：

```json
{
  "code": 0,
  "msg": "系统异常",
  "data": null
}
```

---

## 附录一：接口汇总

| 编号 | Path | Method | 认证 | 说明 |
|------|------|------|------|------|
| 11 | `/api/app/home` | GET | 🔓 | 首页聚合数据（Banner + 热门展览 + 热门美术馆） |
| 12 | `/api/app/galleries` | GET | 🔓 | 美术馆列表（支持搜索） |
| 13 | — | — | — | "艺览智荐"推荐展览 — **待定** |

---

## 附录二：接口与数据库表对应

| 编号 | Path | 数据库操作 |
|------|------|------|
| 11 | `/api/app/home` | `banner` SELECT（按 sort DESC）+ `exhibition` SELECT（is_hot=1, status=1）+ `gallery` SELECT（status=0） |
| 12 | `/api/app/galleries` | `gallery` SELECT（支持 city + keyword 筛选） |
| 13 | 待定 | AI 推荐，依赖用户行为数据（浏览量/收藏），后端提供基础展览数据给 AI 侧 |

## 附录三：前端页面对应接口

| 前端页面 | 调用接口 |
|---------|---------|
| 首页首屏加载 | `/api/app/home` |
| 首页"艺览智荐"板块 |  |
| 首页底部"热门美术馆"— 查看更多 | `/api/app/galleries` |
| 独立美术馆列表页 | `/api/app/galleries` |

