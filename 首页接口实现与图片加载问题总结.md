# 首页展览接口 — 前端实现与问题排查总结

> 文档日期：2026-08-03

---

## 一、前端接口实现

### 1.1 架构概览

前端采用 **Vue 3 + TypeScript + Axios** 技术栈，遵循项目既有的分层模式：

```
src/
├── types/index.ts      ← 类型定义（与后端 VO 一一对应）
├── api/
│   ├── request.ts      ← Axios 实例（统一拦截器）
│   ├── home.ts         ← 首页 API（新增）
│   ├── auth.ts         ← 登录/注册 API
│   └── profile.ts      ← 个人信息 API
├── views/
│   └── HomeView.vue    ← 首页视图（重写）
└── stores/
    └── auth.ts         ← 登录状态管理
```

### 1.2 新增/修改文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `src/types/index.ts` | 修改 | 新增 5 个首页相关类型 |
| `src/api/home.ts` | 新增 | 首页 API 模块 |
| `src/views/HomeView.vue` | 重写 | 完整首页视图 |

---

### 1.3 类型定义（`src/types/index.ts`）

依据接口文档 `02首页展览接口文档.md` 的返回字段，新增以下 TypeScript 接口：

```typescript
/** 轮播图 Banner */
interface Banner {
  id: number
  imageUrl: string     // 对应后端 imageUrl (camelCase)
  title: string
}

/** 热门展览 */
interface HotExhibition {
  id: number
  posterImage: string   // 对应后端 posterImage
  title: string
  subtitle: string
  galleryName: string   // 来自联表 gallery.name
  type: number
}

/** 美术馆 */
interface Gallery {
  id: number
  name: string
  coverImage: string
  address: string
  exhibitionCount: number
  type: number
}

/** 首页聚合数据 */
interface HomeData {
  banners: Banner[]
  hotExhibitions: HotExhibition[]
  galleries: Gallery[]
}

/** 美术馆列表查询参数 */
interface GalleryQuery {
  city?: string
  keyword?: string
}
```

**设计要点：**
- 字段名与后端 JSON 的 camelCase 完全一致，无需额外映射
- `HomeData` 结构对应 `GET /api/app/home` 的 `data` 字段
- 所有类型复用 `ApiResponse<T>` 泛型包装

---

### 1.4 API 模块（`src/api/home.ts`）

```typescript
import request from './request'
import type { ApiResponse, HomeData, Gallery, GalleryQuery } from '../types'

/** GET /api/app/home — 首页聚合数据 */
export function getHomeData(city?: string): Promise<ApiResponse<HomeData>> {
  return request.get('/api/app/home', { params: { city } }).then(res => res.data)
}

/** GET /api/app/galleries — 美术馆列表 */
export function getGalleries(params?: GalleryQuery): Promise<ApiResponse<Gallery[]>> {
  return request.get('/api/app/galleries', { params }).then(res => res.data)
}
```

**设计要点：**
- 复用 `request.ts` 的 Axios 实例（已配置 `baseURL: http://localhost:8080`、自动带 Token、统一错误处理）
- `city` 参数可选，不传则返回全部城市数据
- 返回 `.then(res => res.data)` 将 AxiosResponse 解包为 `ApiResponse<T>`
- 与 `auth.ts`、`profile.ts` 的风格保持一致

---

### 1.5 首页视图（`src/views/HomeView.vue`）

#### 页面结构

```
┌──────────────────────────────────┐
│  顶部导航 (sticky)                │
│  Logo  |  搜索  |  登录/注册      │
├──────────────────────────────────┤
│  Banner 轮播 (自动播放/手动切换)   │
├──────────────────────────────────┤
│  艺览智荐 (AI推荐 — 占位待定)      │
├──────────────────────────────────┤
│  当前热门展览 (横向滚动卡片)       │
├──────────────────────────────────┤
│  热门美术馆 (Grid 网格)           │
└──────────────────────────────────┘
```

#### 状态管理

| 状态 | 触发条件 | UI 表现 |
|------|---------|---------|
| **加载中** | `loading === true` | CSS Spinner + "加载中..." |
| **加载失败** | API 异常 / 网络错误 | 错误信息 + "重试"按钮 |
| **空数据** | 三个数组全部为空 | "暂无展览内容，敬请期待" |
| **正常展示** | API 返回数据 | 完整首页内容 |
| **图片失败** | 单张图片 404/超时 | 内联 SVG 占位图 |

#### 轮播图交互

- **自动播放**：每 3 秒切换，`setInterval` 实现
- **悬停暂停**：`@mouseenter` 设 `bannerPaused = true`，`@mouseleave` 恢复
- **手动切换**：左右箭头按钮 + 底部圆点指示器
- **组件卸载**：`onUnmounted` 清除定时器，防止内存泄漏

#### 图片加载失败兜底

```typescript
function onImageError(e: Event) {
  const img = e.target as HTMLImageElement
  img.src = 'data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" ...>...</svg>'
}
```

- 绑定在所有 `<img>` 标签的 `@error` 事件上
- 失败后替换为内联 SVG（灰色背景 + "图片加载失败"文字）
- 不依赖任何外部资源，离线环境也能展示

---

### 1.6 Axios 请求/响应链路

```
HomeView.vue                    request.ts                  后端 Spring Boot
    │                               │                             │
    ├─ getHomeData() ──────────────►│                             │
    │                               ├─ 请求拦截：附加 Bearer Token │
    │                               ├─ GET /api/app/home ────────►│
    │                               │                             ├─ JwtInterceptor (放行)
    │                               │                             ├─ HomeController.home()
    │                               │                             ├─ HomeServiceImpl.getHomeData()
    │                               │                             ├─ bannerMapper.selectList()
    │                               │                             ├─ exhibitionMapper.selectHotExhibitions()
    │                               │                             ├─ galleryMapper.selectList()
    │                               │                             ├─ 组装 HomeVO
    │                               │  ◄────── JSON ──────────────┤
    │                               ├─ 响应拦截：code≠1 → reject  │
    │  ◄── ApiResponse<HomeData> ──┤                             │
    │                               │                             │
    ├─ 解构 banners/hotExhibitions/galleries                      │
    ├─ 更新响应式状态                                              │
    └─ 渲染模板                                                   │
```

---

## 二、图片加载失败问题排查

### 2.1 问题现象

首页所有图片（Banner 轮播、展览海报、美术馆封面）均加载失败，前端降级显示灰色占位图（"图片加载失败"）。

### 2.2 排查过程

```
问题：图片加载失败
  │
  ├─ 第1步：检查前端请求
  │   └─ 结论：API 返回 code=1，数据正常，每个对象都包含图片 URL 字段
  │
  ├─ 第2步：检查图片 URL 内容
  │   └─ 发现：所有图片 URL 均指向 https://oss.example.com/...
  │            oss.example.com 是不存在的示例域名，DNS 解析失败
  │
  ├─ 第3步：检查数据库
  │   └─ 确认：banner.image_url、exhibition.poster_image、gallery.cover_image
  │            全部存储的是占位 URL（建表后插入的测试数据使用）
  │
  └─ 第4步：检查 OSS 配置
      └─ 发现：application.yml 中阿里云 OSS 配置信息（endpoint 除外）均为占位符
             access-key-id: "<你的阿里云AccessKey ID>"
             access-key-secret: "<你的阿里云AccessKey Secret>"
             且数据库中也未录入真实 OSS 地址
```

### 2.3 根因

**核心原因：后端接口返回的图片 URL 指向了一个不存在的域名 `oss.example.com`，浏览器无法建立连接，导致图片加载失败。**

具体来说：

| 链路节点 | 实际情况 | 结果 |
|---------|---------|------|
| 后端 API | `GET /api/app/home` 正常返回 `code=1`，数据完整 | ✅ 正常 |
| 图片 URL | `https://oss.example.com/banner_waterprint.jpg` | ❌ 域名不存在 |
| DNS 解析 | `oss.example.com` → 解析失败 | ❌ 无法连接 |
| 数据库 | `banner.image_url` 等字段存储的是示例 URL | ❌ 占位数据 |

> **结论：不是代码逻辑问题。后端接口功能正常，但数据库中图片字段存储的是不可访问的占位 URL。**

### 2.4 前端已有的防护措施

前端 `HomeView.vue` 已对所有 `<img>` 标签绑定 `@error` 事件处理，当图片加载失败时自动替换为内联 SVG 占位图：

```typescript
// src/views/HomeView.vue 第 97-101 行
function onImageError(e: Event) {
  const img = e.target as HTMLImageElement
  // 用 data URI 内联 SVG 替换，不依赖任何外部资源
  img.src = 'data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" ...>图片加载失败</svg>'
}
```

这意味着即使图片全部无法加载，页面结构也不会变形，用户能看到灰色占位图和文字提示。

### 2.5 解决方案

> **以下方案均为仅修改数据库数据，不涉及后端代码改动。**

---

#### 方案 A：替换为可公开访问的图片 URL（推荐用于开发测试）

直接更新数据库中图片字段，换成真实存在的图片地址：

```sql
-- 使用 picsum.photos 提供的随机图片作为临时占位
UPDATE banner
SET image_url = 'https://picsum.photos/1200/500?random=1'
WHERE id = 1;

UPDATE banner
SET image_url = 'https://picsum.photos/1200/500?random=2'
WHERE id = 2;

UPDATE exhibition
SET poster_image = 'https://picsum.photos/400/280?random=3'
WHERE id = 1;

UPDATE exhibition
SET poster_image = 'https://picsum.photos/400/280?random=4'
WHERE id = 2;

UPDATE gallery
SET cover_image = 'https://picsum.photos/600/360?random=5'
WHERE id = 1;

UPDATE gallery
SET cover_image = 'https://picsum.photos/600/360?random=6'
WHERE id = 2;
```

**优点**：立即生效，不需要任何后端改动
**缺点**：依赖外部服务，图片内容与展览无关

---

#### 方案 B：使用本地静态图片

1. 将图片文件放入 `server-side/cityart-server/src/main/resources/static/images/` 目录
2. 更新数据库 URL 为后端本地地址：

```sql
UPDATE banner
SET image_url = 'http://localhost:8080/images/banner_1.jpg'
WHERE id = 1;
```

**优点**：不依赖外部网络，完全自主可控
**缺点**：需要准备图片素材

---

#### 方案 C：配置阿里云 OSS 并使用真实图片（推荐用于生产）

1. 在 `application.yml` 中填入真实的阿里云 OSS 凭证
2. 手动上传图片到 OSS 控制台
3. 将获得的 OSS 公开 URL 更新到数据库

```sql
UPDATE banner
SET image_url = 'https://cityart-gz.oss-cn-guangzhou.aliyuncs.com/banner/xxx.jpg'
WHERE id = 1;
```

**优点**：生产就绪，图片托管在 CDN，加载速度快
**缺点**：需要阿里云账号和 OSS 配置

---

#### 方案 D：使用 base64 内联图片（适合少量小图）

将小尺寸图片转为 base64 字符串直接存入数据库：

```sql
UPDATE banner
SET image_url = 'data:image/jpeg;base64,/9j/4AAQSkZJRg...'
WHERE id = 1;
```

**优点**：零外部依赖，不产生额外 HTTP 请求
**缺点**：base64 字符串极长，适合头像等小图；数据库存储膨胀

---

### 2.6 推荐操作顺序

```
开发阶段：  方案 A（picsum 公开图）→ 立即可看到页面效果
           │
           ▼
联调阶段：  方案 B（本地图片）→ 前后端联调验证完整链路
           │
           ▼
上线阶段：  方案 C（阿里云 OSS）→ 生产环境正式使用
```

---

## 三、补充发现：后端"系统异常"问题

### 3.1 问题

`GET /api/app/home` 和 `GET /api/app/galleries` 返回 `{"code":0,"msg":"系统异常"}`。

### 3.2 根因

数据库 `city_art` 中缺少 `banner`、`exhibition`、`gallery` 三张表。建表 SQL 脚本在 commit `42249fa` 中被删除（`Delete server-side/database directory`），导致这些表从未被创建。

### 3.3 解决

根据实体类（`Banner.java`、`Exhibition.java`、`Gallery.java`）的 `@TableField` 注解还原建表 DDL 并执行：

```sql
CREATE TABLE banner (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    image_url VARCHAR(500) NOT NULL,
    title VARCHAR(200),
    sort INT DEFAULT 0,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE gallery (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(200) NOT NULL,
    cover_image VARCHAR(500),
    address VARCHAR(500),
    city VARCHAR(50),
    district VARCHAR(50),
    intro TEXT,
    exhibition_count INT DEFAULT 0,
    status INT DEFAULT 0,
    type INT DEFAULT 0,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

CREATE TABLE exhibition (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    gallery_id BIGINT NOT NULL,
    title VARCHAR(200) NOT NULL,
    subtitle VARCHAR(300),
    poster_image VARCHAR(500),
    start_date DATE,
    end_date DATE,
    description TEXT,
    is_hot INT DEFAULT 0,
    sort_order INT DEFAULT 0,
    status INT DEFAULT 0,
    type INT DEFAULT 0,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_gallery_id (gallery_id),
    INDEX idx_status (status),
    INDEX idx_is_hot (is_hot)
);
```

---

## 四、项目文件对照速查

| 文档/需求 | 前端文件 | 后端文件 |
|-----------|---------|---------|
| 02首页展览接口文档.md | `src/api/home.ts` | `HomeController.java` |
| 美术前端需求改3.md §2.1 | `src/views/HomeView.vue` | `HomeServiceImpl.java` |
| Banner 实体 | `types/index.ts` → `Banner` | `Banner.java` / `BannerMapper.java` |
| 展览实体 | `types/index.ts` → `HotExhibition` | `Exhibition.java` / `ExhibitionMapper.java` |
| 美术馆实体 | `types/index.ts` → `Gallery` | `Gallery.java` / `GalleryMapper.java` |
| 轮播图交互 | `HomeView.vue` (banner logic) | — |
| 图片兜底 | `HomeView.vue` → `onImageError()` | — |
| 全局异常处理 | `request.ts` → 响应拦截器 | `GlobalExceptionHandler.java` |

---

## 五、待办事项

| 优先级 | 事项 | 说明 |
|--------|------|------|
| 🔴 | 配置阿里云 OSS 凭证 | 替换 `application.yml` 中的占位符 |
| 🔴 | 替换数据库图片 URL | 将 `oss.example.com` 替换为真实可用地址 |
| 🟡 | 实现图片上传接口 | `POST /api/common/upload` → OSS → 返回 URL |
| 🟡 | 封装 `<SafeImage>` 组件 | 统一处理加载中、失败、占位图三种状态 |
| 🟡 | 补充建表 SQL 脚本 | 保存到 `server-side/database/` 目录并加入版本控制 |
| 🟢 | "艺览智荐"AI 推荐板块 | 接口文档标注"待定"，需与 AI 团队确认方案 |

---

## 六、前端页面效果说明

### 当前实现完成的首页板块

| 板块 | 数据来源 | 交互 |
|------|---------|------|
| 顶部导航 | `authStore`（登录态） | 未登录显示登录/注册，已登录显示头像+退出 |
| Banner 轮播 | `GET /api/app/home` → `banners` | 自动播放(3s)、悬停暂停、左右箭头、圆点导航 |
| 艺览智荐 | — | 占位提示"登录后获取专属推荐内容" |
| 热门展览 | `GET /api/app/home` → `hotExhibitions` | 横向滚动、悬停上浮、点击跳转详情 |
| 热门美术馆 | `GET /api/app/home` → `galleries` | Grid 网格、悬停阴影、点击跳转、"查看更多"入口 |

### 覆盖的状态场景

- ✅ 数据加载中（Spinner）
- ✅ 网络请求失败（错误提示 + 重试按钮）
- ✅ 后端返回空数组（"暂无展览内容"）
- ✅ 单张图片加载失败（SVG 占位图）
- ✅ 用户未登录/已登录（导航栏动态切换）
- ✅ 轮播图只有一张时（不启动自动播放）
